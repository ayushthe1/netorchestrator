server:
  port: 8080
  mode: release

database:
  host: localhost
  port: 5433
  dbname: netorch_mvp
  user: netorch_admin
  password: ""
  sslmode: disable
  max_connections: 25
  max_idle_connections: 5

redis:
  host: localhost
  port: 6379
  db: 0
  password: ""
  
security:
  jwt_secret: ""
  api_rate_limit: 100
  enable_cors: true
